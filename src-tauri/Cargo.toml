[package]
name = "photoshop"
version = "0.1.0"
edition = "2021"
description = "Minimal Photoshop-style MVP built with Tauri"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
